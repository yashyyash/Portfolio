name: Deploy Portfolio to GitHub Pages

on:
  push:
    branches:
      - master  # or 'main' if that's your default branch

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      id-token: write  # Required permission to allow OIDC-based token request

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Deploy to GitHub Pages
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        branch: gh-pages  # Deploy to the gh-pages branch
        folder: ./  # Root folder of the repository (assuming static files)
        token: ${{ secrets.GITHUB_TOKEN }}  # GitHub's secret token for actions


        #yaar ye necessary comment hai 